# 📐 Правила работы со схемами БД (PostgreSQL ←→ SQLite ←→ ORM)

Этот каталог — **единственный источник истины** для структуры базы данных.
Любые изменения схемы делаются строго по этому регламенту.

---

# Процесс обновления схемы 

## 1. Изменить PostgreSQL-схему
Редактируем **schema_psql.sql** вручную.

Запрещено:
- использовать SERIAL, BOOLEAN, JSONB, NUMERIC — только чистые типы
- использовать PSQL-функции (now(), uuid_generate(), triggers)
- вносить изменения только в SQLite-версию

PSQL — основная схема, SQLite — её отражение.

---

## 2. Синхронизировать schema_sqlite.sql
Привести **schema_sqlite.sql** к эквиваленту PSQL:

- SERIAL → INTEGER PRIMARY KEY AUTOINCREMENT  
- BOOLEAN → INTEGER (0/1)  
- NUMERIC → REAL  
- JSONB → TEXT  
- TIMESTAMP DEFAULT now() → TEXT DEFAULT CURRENT_TIMESTAMP  
- DATE → TEXT  

SQLite-версия должна полностью повторять структуру PostgreSQL, но в допустимых типах.

---

## 3. Пересоздать временную SQLite-базу

```bash
rm -f data/sqlite/booking.db
sqlite3 data/sqlite/booking.db < schema/schema_sqlite.sql


## 4. Перегенерировать ORM-модели

> ORM полностью перезаписывается. Вручную его не редактируем.

```
sqlacodegen sqlite:///data/sqlite/booking.db \
    --outfile backend/app/models/generated.py
```

## TO DO
- добавить CI-проверку схемы,
- сделать Makefile,
- добавить автогенерацию SQLite + ORM одной командой.
