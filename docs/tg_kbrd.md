# UI‑контракт клавиатуры Telegram‑бота (Reply / Inline)

> **Источник истины** для всей логики меню и навигации.
> Любые изменения в коде **обязаны** соответствовать этому документу.
> 
> **Версия:** 2.0 (проверено на Android + Desktop)

---

## 1. Цели контракта

* ReplyKeyboard **не схлопывается** на Android
* Системная клавиатура (IME) **не появляется**
* Чат остаётся **чистым** (только 1 сообщение бота)
* Поведение **детерминированное** и воспроизводимое

---

## 2. Термины

| Термин                   | Описание                                                      |
| ------------------------ | ------------------------------------------------------------- |
| **ReplyKeyboard**        | Клавиатура Telegram (внизу экрана)                            |
| **InlineKeyboard**       | Кнопки под сообщением (callback)                              |
| **Якорь меню**           | Сообщение бота с ReplyKeyboard                                |
| **last_menu_message_id** | `message_id` якоря в Redis                                    |
| **IME**                  | Системная клавиатура Android                                  |

---

## 3. Критические требования

### 3.1 Обязательные параметры ReplyKeyboard

```python
ReplyKeyboardMarkup(
    keyboard=[...],
    resize_keyboard=True,
    is_persistent=True,  # КРИТИЧНО для Android!
)
```

**`is_persistent=True`** — держит клавиатуру видимой, предотвращает IME.

### 3.2 Текст сообщения

**ZWS (`\u200b`) НЕ РАБОТАЕТ** — Telegram API отклоняет как пустой текст.

Использовать:
- Заголовок меню: `"🏠 Главное меню"`, `"⚙️ Настройки"`
- Минимум: emoji `"📋"`

### 3.3 Хранилище состояния

Redis:
```
key:   tg:menu:{chat_id}
value: message_id (string)
ttl:   нет
```

---

## 4. Type A — Reply → Reply (основная навигация)

**Назначение:** переходы между меню

### Алгоритм (порядок критичен!)

```
1. old_menu_id = Redis.get(chat_id)
2. user_msg_id = message.message_id

3. msg = send_message(title, ReplyKeyboard)     # СНАЧАЛА отправить!
4. Redis.set(chat_id, msg.id)                   # Сохранить новый якорь

5. if old_menu_id: delete(old_menu_id)          # ПОТОМ удалить старый
6. delete(user_msg_id)                          # И сообщение пользователя
```

### Почему такой порядок?

- Шаг 3: клавиатура **появляется** у пользователя
- Шаги 5-6: удаления происходят когда клавиатура **уже активна**
- IME не появляется, т.к. ReplyKeyboard уже на экране

### Результат в чате

```
[ Bot: "🏠 Главное меню" + клавиатура ]
```

Одно сообщение. Чисто.

---

## 5. Type B — Reply → Inline (вход в сценарий)

**Назначение:** подтверждения, выбор параметров, формы

### Алгоритм

```
1. old_menu_id = Redis.get(chat_id)
2. user_msg_id = message.message_id

3. send_message(text, InlineKeyboard)           # Inline сообщение
4. Redis.delete(chat_id)                        # Очистить якорь

5. if old_menu_id: delete(old_menu_id)          # Удалить reply-меню
6. delete(user_msg_id)                          # Удалить сообщение пользователя
```

### Важно

- Inline-сообщение **НЕ** становится якорем
- `last_menu_message_id = null` после перехода в Inline

---

## 6. Type C — Reply → Reply с текстом (FSM/wizard)

**Назначение:** пошаговые сценарии с вопросами

### Алгоритм

```
1. old_menu_id = Redis.get(chat_id)
2. if old_menu_id: delete(old_menu_id)

3. msg = send_message(question_text, ReplyKeyboard)
4. Redis.set(chat_id, msg.id)
```

### Отличие от Type A

- Сообщение пользователя **НЕ удаляется** (нужен контекст)
- Текст содержит вопрос/инструкцию

---

## 7. Inline → Reply (возврат из сценария)

```
1. delete(inline_message)
2. call Type A
```

---

## 8. Строгие запреты

| Запрещено                              | Почему                        |
| -------------------------------------- | ----------------------------- |
| `ReplyKeyboardRemove` между меню       | Триггерит IME                 |
| Удаление ДО отправки нового меню       | Триггерит IME                 |
| ZWS как текст сообщения                | API отклоняет                 |
| `is_persistent=False`                  | Клавиатура схлопывается       |
| Несколько якорей в Redis               | Неопределённое поведение      |

---

## 9. Структура кода

```
bot/app/
├── main.py                    # dp, bot, process_update, route_by_role
├── keyboards/
│   └── admin.py               # ФОРМА: структура кнопок (is_persistent=True)
├── flows/
│   └── admin/
│       └── menu.py            # ЛОГИКА: какое меню показать
├── handlers/
│   └── admin_reply.py         # РОУТИНГ: text → flow action
└── utils/
    └── menucontroller.py      # ТРАНСПОРТ: send/delete, Redis
```

---

## 10. Чеклист для нового кода

- [ ] `is_persistent=True` в клавиатуре?
- [ ] Текст непустой (не ZWS)?
- [ ] Порядок: send → delete?
- [ ] Один якорь в Redis?
- [ ] Inline не трогает якорь?

---

## 11. История изменений

| Версия | Дата       | Изменения                                    |
| ------ | ---------- | -------------------------------------------- |
| 1.0    | 2024-12    | Первоначальный контракт                      |
| 2.0    | 2024-12-26 | ZWS не работает, is_persistent обязателен, порядок send→delete |

TODO:

show_inline_readonly() — Type B1, не удаляет Reply-якорь
show_inline_input() — Type B2, удаляет, IME активен
